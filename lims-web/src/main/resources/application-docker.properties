spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/limsdb}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:lims}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:lims}
spring.jpa.hibernate.ddl-auto=${SPRING_JPA_HIBERNATE_DDL_AUTO:update}

# Production-ready apps should configure connection pools, character sets, and
# secure credentials via a secret manager or environment variables provided
# by your deployment platform.
